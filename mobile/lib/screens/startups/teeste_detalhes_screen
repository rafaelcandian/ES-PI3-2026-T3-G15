import 'package:flutter/material.dart';

class StartupDetailScreen extends StatelessWidget {
  const StartupDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xFF0D1030),
      appBar: AppBar(
        backgroundColor: const Color(0xFF0D1030),
        elevation: 0,
        title: const Text(
          "Detalhes da Oferta",
          style: TextStyle(
            color: Color(0xFFFFC081),
            fontWeight: FontWeight.bold,
          ),
        ),
        iconTheme: const IconThemeData(color: Color(0xFFFFC081)),
        actions: const [
          Icon(Icons.share),
          SizedBox(width: 15),
          Icon(Icons.notifications),
          SizedBox(width: 15),
        ],
      ),

      bottomNavigationBar: Container(
        padding: const EdgeInsets.all(16),
        color: const Color(0xFF0D1030),
        child: Row(
          children: [
            Expanded(
              child: OutlinedButton(
                onPressed: () {},
                child: const Text("Vender tokens"),
              ),
            ),
            const SizedBox(width: 10),
            Expanded(
              flex: 2,
              child: ElevatedButton(
                style: ElevatedButton.styleFrom(
                  backgroundColor: Colors.orange,
                ),
                onPressed: () {},
                child: const Text("Comprar tokens"),
              ),
            ),
          ],
        ),
      ),

      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20),
        child: Column(
          children: [

            // IMAGEM PRINCIPAL
            ClipRRect(
              borderRadius: BorderRadius.circular(20),
              child: Stack(
                alignment: Alignment.center,
                children: [
                  Image.network(
                    "https://images.unsplash.com/photo-1497366754035-f200968a6e72",
                    height: 220,
                    width: double.infinity,
                    fit: BoxFit.cover,
                  ),
                  CircleAvatar(
                    radius: 35,
                    backgroundColor: Colors.orange,
                    child: IconButton(
                      onPressed: () {},
                      icon: const Icon(
                        Icons.play_arrow,
                        color: Colors.white,
                        size: 35,
                      ),
                    ),
                  ),
                ],
              ),
            ),

            const SizedBox(height: 25),

            // TÍTULO
            const Align(
              alignment: Alignment.centerLeft,
              child: Text(
                "SolarStream Tech",
                style: TextStyle(
                  color: Color(0xFFFFC081),
                  fontSize: 30,
                  fontWeight: FontWeight.bold,
                ),
              ),
            ),

            const SizedBox(height: 15),

            // TAGS
            Wrap(
              spacing: 10,
              children: [
                _buildTag("Energia Limpa"),
                _buildTag("SaaS B2B"),
                _buildTag("Série A"),
              ],
            ),

            const SizedBox(height: 25),

            // CARD DE INVESTIMENTO
            Container(
              padding: const EdgeInsets.all(20),
              decoration: BoxDecoration(
                color: const Color(0xFF161839),
                borderRadius: BorderRadius.circular(20),
              ),
              child: Row(
                mainAxisAlignment: MainAxisAlignment.spaceAround,
                children: const [
                  Column(
                    children: [
                      Text(
                        "Valor do Token",
                        style: TextStyle(color: Colors.grey),
                      ),
                      SizedBox(height: 8),
                      Text(
                        "R\$ 1.250,00",
                        style: TextStyle(
                          color: Color(0xFFFFC081),
                          fontSize: 20,
                          fontWeight: FontWeight.bold,
                        ),
                      ),
                    ],
                  ),
                  Column(
                    children: [
                      Text(
                        "Meta",
                        style: TextStyle(color: Colors.grey),
                      ),
                      SizedBox(height: 8),
                      Text(
                        "82%",
                        style: TextStyle(
                          color: Colors.amber,
                          fontSize: 20,
                          fontWeight: FontWeight.bold,
                        ),
                      ),
                    ],
                  ),
                ],
              ),
            ),

            const SizedBox(height: 25),

            // SUMÁRIO
            _buildSectionCard(
              title: "Sumário Executivo",
              child: const Text(
                "A SolarStream Tech está revolucionando a distribuição de energia solar através de algoritmos inteligentes em tempo real.",
                style: TextStyle(color: Colors.white70),
              ),
            ),

            const SizedBox(height: 20),

            // SÓCIOS
            _buildSectionCard(
              title: "Quadro Societário",
              child: Column(
                children: const [
                  _SocioTile("Arthur Meireles", "CEO", "45%"),
                  _SocioTile("Beatriz Silva", "CTO", "25%"),
                  _SocioTile("Ricardo Goulart", "COO", "15%"),
                ],
              ),
            ),

            const SizedBox(height: 20),

            // FAQ
            _buildSectionCard(
              title: "Perguntas Frequentes",
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: const [
                  Text(
                    "Como é feito o resgate do lucro?",
                    style: TextStyle(
                      color: Colors.white,
                      fontWeight: FontWeight.bold,
                    ),
                  ),
                  SizedBox(height: 5),
                  Text(
                    "Os lucros são distribuídos automaticamente.",
                    style: TextStyle(color: Colors.white70),
                  ),
                ],
              ),
            ),

            const SizedBox(height: 80),
          ],
        ),
      ),
    );
  }

  static Widget _buildTag(String text) {
    return Chip(
      label: Text(text),
      backgroundColor: const Color(0xFF303254),
      labelStyle: const TextStyle(color: Colors.amber),
    );
  }

  static Widget _buildSectionCard({
    required String title,
    required Widget child,
  }) {
    return Container(
      width: double.infinity,
      padding: const EdgeInsets.all(20),
      decoration: BoxDecoration(
        color: const Color(0xFF161839),
        borderRadius: BorderRadius.circular(20),
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Text(
            title,
            style: const TextStyle(
              color: Color(0xFFFFC081),
              fontSize: 20,
              fontWeight: FontWeight.bold,
            ),
          ),
          const SizedBox(height: 15),
          child,
        ],
      ),
    );
  }
}

class _SocioTile extends StatelessWidget {
  final String nome;
  final String cargo;
  final String porcentagem;

  const _SocioTile(
    this.nome,
    this.cargo,
    this.porcentagem,
  );

  @override
  Widget build(BuildContext context) {
    return ListTile(
      contentPadding: EdgeInsets.zero,
      leading: const CircleAvatar(
        backgroundColor: Colors.grey,
        child: Icon(Icons.person),
      ),
      title: Text(
        nome,
        style: const TextStyle(color: Colors.white),
      ),
      subtitle: Text(
        cargo,
        style: const TextStyle(color: Colors.white54),
      ),
      trailing: Text(
        porcentagem,
        style: const TextStyle(
          color: Colors.amber,
          fontWeight: FontWeight.bold,
        ),
      ),
    );
  }
}